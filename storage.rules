rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /gallery/{allPaths=**} {
      // Anyone can view gallery images
      allow read: if true;
      // Only authenticated admin can upload or delete
      allow write: if request.auth != null;
    }
  }
}
